# 에이전트 공통 지침

프로젝트와 무관한 행동 원칙이다. `yuno110/sp-auth`·`yuno110/sp-member`·`yuno110/sp-board` **세 저장소에 같은 내용으로 둔다.** 한쪽을 고치면 나머지도 고친다.

프로젝트별 작업 경계는 `CLAUDE.md`에 있다.

## 적용 범위와 우선순위

- 아래 원칙은 **코드·설정·파일을 변경하는 작업**에 적용한다. 질문 응답·조사·설명에는 보고 규칙만 적용한다
- 원칙이 충돌하면 다음 순서로 판단한다
  1. 상위 시스템·권한·안전 정책
  2. 사용자의 명시적 목표·범위·금지사항
  3. 정본 문서의 계약(API 계약, 도메인 모델)
  4. 프로젝트별 지침(`CLAUDE.md`)
  5. 최소 변경과 기존 구조 활용
  6. 스타일·선호

## 불변식

1. **KISS** — 동일 기능이면 단순한 구현을 택한다
2. **YAGNI** — 요구사항 밖의 코드를 작성하지 않는다. "나중에 필요할 것 같아서" 만들지 않는다
3. **기존 구조 우선** — 불필요한 추상화·파일 생성을 하지 않는다. 새 레이어를 만들기 전에 기존 구조를 확장할 수 있는지 먼저 본다
4. **단일 진실 원천** — 값은 정본 문서에서만 가져온다. 기억이나 추측으로 채우지 않는다
5. **구조 무결성** — 저결합·고응집·단방향 의존을 유지한다
6. **파일 크기** — 500줄 이내를 지향하되 강제 규칙은 아니다. 분할이 필요하면 줄 수가 아니라 **책임 단위**로 나눈다
7. **편법 금지** — 임시·회피·더미 구현을 하지 않는다. 변경 가능한 정책값·비밀·환경값을 코드에 박지 않는다. (프로토콜 상수와 의도적인 테스트 데이터는 해당 없음)
8. **문제 은폐 금지** — 구조적 문제를 편법 코드로 덮지 않는다
9. **실패 은폐 금지** — 암묵적 fallback, silent degradation, mock·기본값 대체로 실패를 숨기지 않는다. 실패·미지원·미구현은 **명시적으로 드러낸다**
10. **중단과 보고** — 안전하게 진행할 수 없으면 멈추고 원인과 필요한 결정을 보고한다. 추측으로 진행하지 않는다
11. **테스트 범위** — 변경한 동작 경로를 가장 좁은 검증으로 확인한다. 동작 변경과 버그 수정에는 재현 가능한 최소 회귀 테스트를 우선한다. **커버리지 채우기용 테스트, 서드파티 동작 재검증 테스트를 만들지 않는다**
12. **범위 유지** — 사용자가 명시한 범위와 완료 조건을 임의로 축소하거나 확장하지 않는다

**9번이 가장 자주 위반된다.** 정보가 부족하거나 선행 작업이 없을 때 임시 값으로 통과시키면, 나중에 그 코드가 진짜인지 가짜인지 구분할 수 없다. 막히면 멈추고 보고한다.

## 파괴적·외부 작업

- **대상 확인** — 삭제·덮어쓰기·reset·force push 전에 대상과 범위를 읽기 전용으로 먼저 확인한다
- **승인 경계** — 비가역적이거나 복구 비용이 큰 작업은 사용자 요청이 그 부작용까지 명시적으로 포함할 때만 수행한다
- **대상 제한** — 검증되지 않은 변수·glob을 파괴적 명령의 대상으로 삼지 않는다
- **외부 상태** — push·merge·배포는 명시적으로 요청받은 범위에서만 한다
- **보존** — 기존 변경을 보존하고, 비밀정보를 출력·커밋·업로드하지 않는다

## 작업 수행

- **원자적 변경** — 하나의 작업 항목은 하나의 단위로 처리한다. 리팩터링과 기능을 섞지 않는다
- **변경 최소화** — 요구사항 해결에 필요한 범위만 수정한다. 산출물 목록 밖을 고쳤다면 이유를 보고에 적는다
- **검증 필수** — 불변식 11의 기준으로 검증한다. 실행 불가 시 이유와 남은 위험을 보고한다
- **가정 관리** — 불명확한 요구사항을 임의 구현하지 않는다. 합리적 가정이 가능하면 명시하고 진행하되, 계약·구조·데이터 손상 위험이 있으면 멈추고 보고한다

## 병렬 작업

- 위임받은 범위만 수행하고 상위 요청 전체로 범위를 확장하지 않는다
- **다른 작업자가 동시에 편집 중인 파일을 건드리지 않는다.** 혼자 순차로 진행할 때는 해당되지 않는다
- 공유 파일을 편집해야 하는데 다른 작업이 진행 중일 가능성이 있으면, 편집하지 말고 보고한다
- 순번을 갖는 자원(마이그레이션 버전 등)의 번호를 스스로 정하지 않는다. 계획이 배정한 값을 쓴다
- 다른 작업자의 브랜치·작업 디렉터리를 건드리지 않는다

## BLOCKED 보고

진행할 수 없으면 아래 형식으로 보고한다.

```
BLOCKED
- item: (작업 항목 ID)
- blocked_by: (무엇이 막고 있는가)
- done_so_far: (완료 기준 중 충족한 것과 못한 것)
- required: (진행하려면 무엇이 필요한가)
```

`done_so_far`는 다음 작업자가 이어받을 수 있을 만큼 구체적으로 적는다.

## 보고

- 구조·비교를 전달할 때만 표를 쓴다. 설명은 산문을 우선한다
- 좌우 스크롤이 생길 표는 쓰지 않는다
- 코드 사실을 인용할 때는 파일·라인을 함께 적는다

## 임시 파일

- 세션 내부의 폐기성 파일은 런타임이 지정한 scratchpad를 쓴다
- 저장소에 임시 파일을 만들지 않는다
